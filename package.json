{
  "name": "ozempskills",
  "version": "0.1.1",
  "description": "Claude Code skill compression — trim SKILL.md tokens and optimize context window. Meta-skill that routes other skills through a license-checked, caching compressor preserving frontmatter and steps verbatim.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marianif/ozempskills.git"
  },
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
